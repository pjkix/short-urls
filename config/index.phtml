<?php
// do some sweet config loading or writing or configing here ... 

// define name and path to config file
define ('CONFIG_FILE', './conf/local.config.yaml');

// read configuration
if (file_exists(CONFIG_FILE)) {
    $CONFIG = syck_load(file_get_contents(CONFIG_FILE));
}

// if no local config load default config then prompt with form for changes


// if everything works write out the local config file ... 


// save to config file
if (file_put_contents(CONFIG_FILE, syck_dump($CONFIG))) {
    echo 'Configuration successfully saved!';
} else {
    echo 'Configuration could not be saved!';
}

// autoloading ... 


function sample_autoloader($class) {
    require str_replace('_', '/', $class) . '.php';
}
spl_autoload_register('sample_autoloader');


?>